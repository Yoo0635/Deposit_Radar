# 2025 새싹 해커톤 | AI 서비스 기능 명세서

**팀명**: F4  
**팀원 정보**: 유인근, 소민섭, 목원주, 노진산  
**테이블 번호**: B-5

---

## 1. 서비스 개요 (Service)

| 항목 | 내용 |
|------|------|
| **서비스명** | 보증금 레이더 |
| **서비스 한줄 소개** | 등기부 변동을 자동 감지하고 위험도·대응 문서를 즉시 제공하는 전세 보증금 보호 서비스 |
| **개발 목표** | 임차인이 놓치기 쉬운 등기 변동을 자동 분석해 보증금 피해를 사전에 차단 |
| **타겟 사용자** | 전세·월세 계약을 앞두거나 이미 거주 중인 모든 임차인, 특히 20~30대 청년층 |
| **기대 효과** | 등기 변동 조기 감지로 전세사기 예방, 위험도·대응 가이드 자동 생성으로 빠른 의사결정 지원과 골든타임 확보, 법률과 등기 해석 부담 감소 |

---

## 2. 시스템 구성도 (Architecture)

```
┌─────────────────────────────────────────────────────────────────┐
│                    [모바일 앱 (React Native/Expo)]                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ 주택 등록     │  │ 등기부 업로드 │  │ 분석 결과 표시│         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ HTTP/REST API
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│              [백엔드 API (FastAPI + PostgreSQL)]                  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 1. 주택 등록 및 관리                                      │    │
│  │    - 계약 정보 저장 (주소, 보증금, 전입일, 확정일자)      │    │
│  │    - 시세 조회 및 저장                                    │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 2. 등기부등본 업로드 및 OCR 처리                            │    │
│  │    POST /upload (FormData: contract_id, files[])          │    │
│  │    ↓                                                       │    │
│  │    [OCR Engine]                                           │    │
│  │    - Clova OCR API (네이버)                               │    │
│  │    - PyMuPDF (PDF → 이미지 변환)                           │    │
│  │    - OpenCV (이미지 전처리: 그레이스케일)                  │    │
│  │    - Regex (텍스트 파싱)                                   │    │
│  │    ↓                                                       │    │
│  │    RegistrySnapshot JSON 생성 (갑구/을구 구조화)          │    │
│  │    ↓                                                       │    │
│  │    PostgreSQL 저장 (JSONB 형식)                           │    │
│  │    ↓                                                       │    │
│  │    초기 LTV 계산 및 저장                                   │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 3. Android 시스템 알림 감지 (등기부 변동 알림)              │    │
│  │    - react-native-android-notification-listener          │    │
│  │    - Headless JS (백그라운드 처리)                        │    │
│  │    - 알림 제목에 "등기부등본" 포함 확인                    │    │
│  │    ↓                                                       │    │
│  │    자동으로 두 번째 스냅샷 생성 요청                      │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 4. 스냅샷 비교 (Diff Engine)                               │    │
│  │    GET /compare/latest/{contract_id}                     │    │
│  │    - 이전 스냅샷 vs 현재 스냅샷 비교                       │    │
│  │    - 갑구/을구 변경사항 추출                               │    │
│  │    - 근저당 증액, 가압류/압류 추가, 경매 개시 등 감지      │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 5. 위험도 분석 (Risk Engine)                               │    │
│  │    POST /risk/{contract_id}                               │    │
│  │    - 위험 이벤트 분석 (경매, 압류, 가압류, 근저당 증액 등) │    │
│  │    - 위험도 등급 계산 (HIGH/CRITICAL/MEDIUM/LOW)          │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 6. LTV 계산 (Loan-to-Value)                                │    │
│  │    - 공식: LTV = (보증금 + 선순위 합계) / 시세 × 100       │    │
│  │    - 위험도 분류:                                         │    │
│  │      * GREEN: LTV < 60% (안전)                            │    │
│  │      * AMBER: 60% ≤ LTV < 80% (주의)                      │    │
│  │      * RED: LTV ≥ 80% (위험)                              │    │
│  │    - 초기 LTV는 첫 번째 스냅샷 생성 시 계산 및 저장       │    │
│  │    - 보증금 변경 시 동적으로 재계산                        │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 7. 회수 금액 분석 (Recovery Engine) ⭐ 새로 추가!          │    │
│  │    - 매각 예측가 계산: 시세 × 85% (경매 시 15% 하락 가정) │    │
│  │    - 선순위 채권 합계 계산 (근저당, 가압류, 압류 등)       │    │
│  │    - 임차인 회수 가능액: 매각 예측가 - 선순위 합계        │    │
│  │    - 실제 회수 금액: min(보증금, 회수 가능액)              │    │
│  │    - 손실 금액: 보증금 - 실제 회수 금액                   │    │
│  │    - 회수 위험도 분류:                                    │    │
│  │      * RED: 손실 > 보증금의 50%                           │    │
│  │      * AMBER: 손실 발생 (0 < 손실 ≤ 보증금의 50%)          │    │
│  │      * GREEN: 손실 없음                                   │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 8. 자동 대응 플레이북 생성 (LLM Engine)                    │    │
│  │    POST /generate-report                                 │    │
│  │    - OpenAI GPT-4o-mini (fallback: gpt-5-nano)           │    │
│  │    - 위험도 등급별 맞춤형 가이드북 생성                    │    │
│  │    - 생성 내용:                                          │    │
│  │      * 금액 회수 분석 (Recovery Engine 결과)              │    │
│  │      * 회수 금액 산정 방식 및 대응 가이드라인             │    │
│  │      * 상황 진단 요약                                    │    │
│  │      * 세부 대응 체크리스트 (5단계)                      │    │
│  │      * 법률 용어 설명 (3-4개)                            │    │
│  │      * 예상 질문 & 답변 (Top 5)                           │    │
│  │      * 임대인 발송용 문자 초안                            │    │
│  │    ↓                                                       │    │
│  │    [PDF 생성 엔진 (WeasyPrint)]                           │    │
│  │    - HTML 템플릿 기반 PDF 생성                             │    │
│  │    - 페이지 분할 방지 처리                                │    │
│  │    - 위험도 등급별 색상 적용 (RED/AMBER/GREEN)            │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │ 9. 결과 반환                                              │    │
│  │    - PDF 파일 URL                                         │    │
│  │    - 위험도 등급 (RED/AMBER/GREEN)                        │    │
│  │    - 분석 기록 저장                                       │    │
│  └──────────────────────────────────────────────────────────┘    │
│                            │                                      │
│                            ▼                                      │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ JSON Response
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    [모바일 앱 (React Native/Expo)]                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ 분석 결과 표시│  │ PDF 다운로드 │  │ 위험도 배지   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 핵심 기능 명세 (Feature Specification)

| 기능명 | 기능 설명 | 입력/출력 데이터 | 관련 기술 및 알고리즘 | 구현 여부 |
|--------|----------|-----------------|---------------------|----------|
| **주택 등록 및 관리** | 전세 계약 정보를 등록하고 관리하는 기능 | **입력**: 주소, 보증금, 전입일, 확정일자<br>**출력**: 계약 ID, 초기 LTV, 시세 정보 | FastAPI, PostgreSQL, SQLAlchemy ORM, 국토부 시세 API | O |
| **등기부 OCR 및 정규화** | 이미지 형식의 등기부 등본을 분석하여 갑구, 을구 구조를 가진 JSON으로 변환 | **입력**: 등기부등본 이미지/PDF 파일<br>**출력**: RegistrySnapshot JSON (갑구/을구 구조화) | Clova OCR API, PyMuPDF, Regex, OpenCV (그레이스케일 전처리) | O |
| **등기부 변경 이력 비교** | 특정 주소지의 과거 등기부 스냅샷과 현재 스냅샷을 비교하여 변경된 내용(소유권, 채무 등)을 식별 | **입력**: RegistrySnapshot 2개 (old, new)<br>**출력**: DiffResult JSON (변경사항 목록) | DeepDiff 알고리즘, 커스텀 비교 로직 | O |
| **부동산 위험도 분석 (Risk Engine)** | LTV(주택담보대출비율), 선순위 채권액, 소유권 변경 이력 등을 기반으로 보증금 반환의 위험도를 계산 | **입력**: RegistrySnapshot, 부동산 시세, 보증금<br>**출력**: 위험 등급(HIGH/CRITICAL/MEDIUM/LOW), 위험 이벤트 목록 | 위험 이벤트 패턴 매칭, 가중치 기반 점수 계산 | O |
| **LTV 계산** | 보증금, 선순위 담보총액, 시세를 기반으로 LTV를 계산하고 위험도 평가 | **입력**: 보증금, 선순위 합계, 시세<br>**출력**: LTV 값(%), 위험도(GREEN/AMBER/RED) | LTV 공식: (보증금 + 선순위 합계) / 시세 × 100<br>위험도 분류: GREEN(<60%), AMBER(60-80%), RED(≥80%) | O |
| **회수 금액 분석 (Recovery Engine)** ⭐ | 경매 시 예상 회수 금액과 손실 금액을 계산하여 회수 위험도 평가 | **입력**: 보증금, 시세, 등기부 을구 정보<br>**출력**: 매각 예측가, 선순위 합계, 회수 가능액, 실제 회수 금액, 손실 금액, 회수 위험도(RED/AMBER/GREEN) | 경매 하락률 가정(15%), 선순위 채권 추출 알고리즘, 회수 금액 계산 공식 | O |
| **자동 대응 플레이북 생성** | 계산된 위험도에 따라 임대인에게 보낼 질의서, 계약 전 체크리스트, 보호자 요약 보고서 등을 자동 생성 | **입력**: 위험 분석 리포트, Recovery Engine 결과<br>**출력**: HTML, PDF 파일 (가이드북) | OpenAI GPT-4o-mini, WeasyPrint (PDF 생성), HTML 템플릿 엔진 | O |
| **Android 시스템 알림 감지** | 등기부 변동 알림을 자동으로 감지하여 분석 프로세스 시작 | **입력**: Android 시스템 알림<br>**출력**: 알림 데이터 (앱명, 제목, 내용), 분석 요청 트리거 | react-native-android-notification-listener, Headless JS, AsyncStorage | O |
| **자동 스냅샷 생성** | 알림 감지 시 자동으로 두 번째 스냅샷을 생성하여 비교 가능하도록 함 | **입력**: 계약 ID<br>**출력**: 두 번째 스냅샷 (근저당 금액 증가, 보증금 조정) | 동적 보증금 계산 (LTV 60-80% 범위 달성), 스냅샷 생성 로직 | O |

---

## 4. 주요 기능 흐름도 (Flow)

### 4.1 서비스 이용 시나리오

#### 시나리오 1: 계약 전 등기부등본 분석

1. **회원가입/로그인**
   - 사용자는 모바일 앱을 통해 서비스에 가입하고 로그인합니다.
   - 간단한 인증 시스템으로 빠른 접근이 가능합니다.

2. **주택 등록**
   - 사용자는 전세 계약을 앞두고 있는 부동산의 정보를 입력합니다.
   - **필수 입력**: 주소, 보증금
   - **선택 입력**: 전입일, 확정일자 (계약 후 입력 가능)
   - 주소는 자동으로 지번 주소로 변환되어 시세 조회에 사용됩니다.

3. **등기부등본 업로드**
   - 사용자는 등기부등본을 사진 촬영 또는 PDF 파일로 업로드합니다.
   - **지원 형식**: 이미지 (최대 2장), PDF (1개)
   - 업로드된 파일은 자동으로 OCR 처리됩니다.

4. **자동 분석 (OCR → 스냅샷 저장 → 초기 LTV 계산)**
   - **OCR 처리**: 
     - Clova OCR API를 통해 텍스트 정보가 추출됩니다.
     - 이미지는 OpenCV로 그레이스케일 전처리 후 OCR 처리됩니다.
     - PDF는 PyMuPDF로 이미지 변환 후 OCR 처리됩니다.
   - **정규화**: 
     - 정규표현식 기반 파싱으로 등기부 데이터가 구조화됩니다.
     - RegistrySnapshot JSON 형식으로 변환됩니다 (갑구/을구 구조).
   - **데이터 저장**: 
     - 변환된 데이터는 '스냅샷'으로 PostgreSQL 데이터베이스에 JSONB 형식으로 저장됩니다.
   - **초기 LTV 계산**: 
     - 담보총액 계산 (을구에서 선순위 담보 추출)
     - 시세 조회 (국토부 실거래가 API 또는 기본값)
     - LTV 계산: (보증금 + 선순위 합계) / 시세 × 100
     - 초기 LTV 및 위험도가 계약 정보에 저장됩니다.

5. **결과 확인**
   - 사용자는 모바일 앱을 통해 분석 결과를 확인합니다.
   - **표시 내용**:
     - 초기 LTV 값 및 위험도 배지 (GREEN/AMBER/RED)
     - 등기부등본 정보 요약
   - 위험도가 높을 경우 (RED/AMBER) 대응 가이드북을 즉시 생성할 수 있습니다.

#### 시나리오 2: 계약 후 등기부 변동 감지 및 분석

1. **등기부 변동 알림 감지**
   - 사용자가 등기부 변동 알림 앱을 설치하고 권한을 부여합니다.
   - Android 시스템 알림을 모니터링하는 Headless JS 태스크가 백그라운드에서 실행됩니다.
   - 등기부 변동 관련 알림이 감지되면 (제목에 "등기부등본" 포함):
     - 알림 내용이 파싱됩니다.
     - 중복 체크가 수행됩니다 (백엔드 DB 기반).
     - 모바일 앱으로 알림이 전달됩니다.

2. **자동 분석 시작**
   - 알림 감지 시 모바일 앱이 자동으로 실행됩니다 (또는 분석 화면으로 이동).
   - 분석 요청 모달이 자동으로 표시됩니다.
   - 사용자가 "분석 시작" 버튼을 클릭하면:

3. **자동 스냅샷 생성**
   - 백엔드에서 자동으로 두 번째 스냅샷을 생성합니다.
   - 첫 번째 스냅샷의 근저당 금액을 2배로 증가시킵니다.
   - 보증금을 동적으로 조정하여 LTV가 AMBER 범위(60-80%)가 되도록 설정합니다.

4. **스냅샷 비교 (Diff Engine)**
   - 이전 스냅샷과 새로 생성된 스냅샷을 비교합니다.
   - 변경사항을 추출합니다:
     - 근저당 증액
     - 가압류/압류 추가
     - 경매 개시
     - 소유권 변경 등

5. **위험도 분석 (Risk Engine)**
   - Diff 결과를 기반으로 위험 이벤트를 분석합니다.
   - 위험도 등급을 계산합니다 (HIGH/CRITICAL/MEDIUM/LOW).
   - 위험 이벤트 목록을 생성합니다.

6. **LTV 재계산**
   - 최신 보증금을 반영하여 LTV를 재계산합니다.
   - 초기 LTV와 비교하여 변화량을 계산합니다.
   - LTV 위험도를 평가합니다 (GREEN/AMBER/RED).

7. **회수 금액 분석 (Recovery Engine)** ⭐
   - 매각 예측가를 계산합니다: 시세 × 85% (경매 시 15% 하락 가정)
   - 선순위 채권 합계를 계산합니다 (근저당, 가압류, 압류 등).
   - 임차인 회수 가능액을 계산합니다: 매각 예측가 - 선순위 합계
   - 실제 회수 금액을 계산합니다: min(보증금, 회수 가능액)
   - 손실 금액을 계산합니다: 보증금 - 실제 회수 금액
   - 회수 위험도를 분류합니다 (RED/AMBER/GREEN).

8. **자동 대응 플레이북 생성 (LLM Engine)**
   - 위험도 등급에 따라 맞춤형 가이드북을 생성합니다.
   - **생성 내용**:
     - 금액 회수 분석 결과 (Recovery Engine)
     - 회수 금액 산정 방식 및 대응 가이드라인
     - 상황 진단 요약 (3문장 이내)
     - 세부 대응 체크리스트 (5단계)
     - 법률 용어 설명 (3-4개)
     - 예상 질문 & 답변 (Top 5)
     - 임대인 발송용 문자 초안
   - OpenAI GPT-4o-mini를 사용하여 생성됩니다.
   - WeasyPrint를 사용하여 PDF로 변환됩니다.

9. **결과 확인 및 대응**
   - 사용자는 모바일 앱을 통해 분석 결과를 확인합니다.
   - **표시 내용**:
     - 위험도 배지 (RED/AMBER/GREEN)
     - 분석 기록 (날짜, 주소, 위험도)
     - PDF 가이드북 다운로드 링크
   - PDF 가이드북을 다운로드하여 활용할 수 있습니다:
     - 임대인에게 보낼 문자 초안
     - 법적 대응 체크리스트
     - 회수 금액 산정 방식 및 대응 가이드라인

10. **알림 정리**
    - PDF 생성 완료 후 알림이 백엔드 DB에서 자동으로 삭제됩니다.
    - 중복 처리를 방지합니다.

---

## 5. 향후 발전 방향 (Future Work)

### 5.1 모델 고도화
- **OCR 인식률 향상**: 더 많은 등기부등본 데이터를 학습하여 OCR 인식률 및 파싱 정확도 향상
- **Risk Engine 정교화**: 분석 규칙을 정교화하여 탐지율을 높이고 오탐지율 감소
- **LLM 프롬프트 최적화**: 더 정확하고 실행 가능한 대응 가이드 생성

### 5.2 데이터 소스 확장
- **국토교통부 실거래가**: 실거래가 데이터를 추가로 연동하여 분석의 신뢰도를 높임
- **법원 경매 정보**: 경매 진행 상황을 실시간으로 모니터링하여 위험도 분석에 반영
- **부동산 시세 API 고도화**: 다양한 시세 소스를 통합하여 더 정확한 시세 정보 제공

### 5.3 기능 확장
- **전세 보증보험 가입 가능 여부 자동 예측**: 보증보험 가입 조건을 분석하여 자동 예측 기능 추가
- **실시간 모니터링 서비스**: 계약 이후 등기부 변동이 생길 시 사용자에게 푸시 알림을 보내주는 실시간 모니터링 서비스 제공
- **다중 주택 관리**: 사용자가 여러 주택을 등록하고 관리할 수 있는 기능
- **분석 기록 필터링/검색**: 과거 분석 기록을 필터링하고 검색할 수 있는 기능

### 5.4 사용자 경험 개선
- **다국어 지원**: 영어, 중국어 등 다국어 지원
- **다크 모드**: 사용자 선호에 따른 다크 모드 지원
- **오프라인 모드**: 네트워크 연결이 없어도 기본 기능 사용 가능
- **알림 설정**: 알림 켜기/끄기, 알림 시간 설정 등 사용자 맞춤 알림 설정

### 5.5 비즈니스 모델
- **프리미엄 서비스**: 고급 분석 기능, 무제한 분석, 우선 지원 등 프리미엄 서비스 제공
- **B2B 서비스**: 부동산 중개업소, 법무법인 등 기업 고객 대상 서비스 제공
- **API 서비스**: 다른 서비스에 위험도 분석 API를 제공하는 B2B2C 모델

---

## 6. 부록 (Appendix)

### 6.1 기술 스택

#### 백엔드
- **프레임워크**: FastAPI (Python)
- **데이터베이스**: PostgreSQL (JSONB 지원)
- **ORM**: SQLAlchemy
- **OCR**: 네이버 Clova OCR API
- **LLM**: OpenAI GPT-4o-mini
- **PDF 생성**: WeasyPrint
- **이미지 처리**: OpenCV, PyMuPDF

#### 프론트엔드
- **프레임워크**: React Native (Expo)
- **라우팅**: Expo Router
- **상태 관리**: React Context API
- **알림 감지**: react-native-android-notification-listener
- **스토리지**: AsyncStorage

#### 인프라
- **서버**: 로컬 개발 환경 (향후 클라우드 배포 예정)
- **API 서버**: FastAPI (uvicorn)
- **정적 파일 서빙**: FastAPI StaticFiles

### 6.2 주요 API 엔드포인트

| 엔드포인트 | 메서드 | 설명 |
|-----------|--------|------|
| `/contracts` | POST | 주택 등록 |
| `/contracts` | GET | 주택 목록 조회 |
| `/contracts/{id}` | GET | 주택 정보 조회 |
| `/contracts/{id}` | PATCH | 주택 정보 수정 |
| `/contracts/{id}` | DELETE | 주택 삭제 |
| `/upload` | POST | 등기부등본 업로드 및 OCR 처리 |
| `/snapshot/{contract_id}` | GET | 스냅샷 조회 |
| `/snapshot/{contract_id}/auto-second` | POST | 자동 두 번째 스냅샷 생성 |
| `/compare/latest/{contract_id}` | GET | 최신 스냅샷 비교 |
| `/risk/{contract_id}` | POST | 위험도 분석 |
| `/generate-report` | POST | PDF 가이드북 생성 |
| `/notifications/check` | POST | 알림 중복 체크 |
| `/notifications/{id}` | DELETE | 알림 삭제 |

### 6.3 데이터베이스 스키마

#### contract_info (계약 정보)
- `id`: 계약 ID (Primary Key)
- `nickname`: 닉네임
- `address`: 주소
- `address_jibun`: 지번 주소
- `deposit`: 보증금
- `move_in_date`: 전입일
- `confirmation_date`: 확정일자
- `market_price`: 시세
- `initial_ltv`: 초기 LTV
- `initial_ltv_risk`: 초기 LTV 위험도
- `initial_total_liens`: 초기 담보총액
- `initial_market_price`: 초기 시세
- `created_at`: 생성일시

#### registry_snapshot (등기부 스냅샷)
- `id`: 스냅샷 ID (Primary Key)
- `contract_id`: 계약 ID (Foreign Key)
- `viewed_at`: 등기부 열람일
- `gabu`: 갑구 정보 (JSONB)
- `eulgu`: 을구 정보 (JSONB)
- `created_at`: 생성일시

#### notification (알림 기록)
- `notification_id`: 알림 ID (Primary Key)
- `app_name`: 앱명
- `title`: 알림 제목
- `text`: 알림 내용
- `created_at`: 생성일시

### 6.4 팀원 역할 분담

- **유인근**: 백엔드 개발 (FastAPI, PostgreSQL, Diff/Risk/Recovery Engine)
- **소민섭**: OCR 엔진 개발 (Clova OCR, 이미지 전처리, 텍스트 파싱)
- **목원주**: 모바일 앱 개발 (React Native, Android 알림 감지, UI/UX)
- **노진산**: LLM 엔진 개발 (OpenAI GPT, PDF 생성, 가이드북 생성)

---

**문서 버전**: 1.0.0  
**최종 수정일**: 2025-01-01  
**작성자**: F4 팀 (유인근, 소민섭, 목원주, 노진산)

